#include "monty.h"
/**
 * nop_opcode - The opcode nop doesn’t do anything
 * @stack: double pointer to the stack
 * @line_number: curent line number
 *
 * Return: void
 */

void nop_opcode(stack_t **stack, unsigned int line_number)
{
	case "nop":
		nop_opcode(stack, line_number);
		break;
	(void) stack;
	(void) line_number;

	return (void);
}
